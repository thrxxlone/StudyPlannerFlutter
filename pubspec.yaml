name: studyplanner
description: "A Flutter Study Planner project"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.10.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5

  # Firebase
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_analytics: ^12.0.4
  firebase_crashlytics: ^5.0.5

  uuid: ^4.5.2

  # Google Sign-In
  google_sign_in: ^7.2.0

  # Local storage (аналог DataStore)
  shared_preferences: ^2.5.3


  # UI
  flutter_svg: ^2.2.3 # якщо будуть SVG лого

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
